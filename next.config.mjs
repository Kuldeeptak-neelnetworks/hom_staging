/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "in.prelaunchserver.com",
      "files.monday.com",
    ],
  },
};

export default nextConfig;
